# Self-Deploy: CI/CD без DevOps

🚀 Автоматическая генерация GitLab CI/CD пайплайнов через анализ структуры Git-репозиториев

## 📋 Описание

Self-Deploy — это интеллектуальный standalone CLI скрипт, который полностью автоматизирует процесс создания CI/CD пайплайнов путем анализа структуры Git-репозитория. Система работает **без внешних LLM запросов**, используя только локальный анализ файлов конфигурации.

### Ключевые возможности

- ✅ **Интерактивный режим** - удобный диалоговый интерфейс для работы
- ✅ **Автоматический анализ** структуры проекта через поиск характерных файлов
- ✅ **Определение технологического стека**: язык, фреймворк, версия, сборщик
- ✅ **Генерация GitLab CI пайплайнов** с полным набором этапов
- ✅ **Multi-stage Docker builds** для оптимизации размера образов
- ✅ **Интеграция с SonarQube** для анализа качества кода
- ✅ **Загрузка артефактов в Nexus** Repository Manager
- ✅ **Кеширование зависимостей** для ускорения сборки
- ✅ **Поддержка 4 базовых языков**: Java/Kotlin, Go, TypeScript/JavaScript, Python

## 🎯 Поддерживаемые языки и экосистемы

### Java/Kotlin
- **Сборщики**: Maven, Gradle
- **Фреймворки**: Spring Boot, Spring Framework, Hibernate
- **Тестирование**: JUnit, Mockito
- **Версии**: Java 8-17, Kotlin 1.6+

### Go
- **Сборщики**: Go Modules
- **Фреймворки**: Gin, Echo, Gorilla Mux, Fiber, GORM
- **Тестирование**: встроенный go test
- **Версии**: Go 1.16+

### TypeScript/JavaScript
- **Сборщики**: npm, yarn, pnpm
- **Фреймворки**: React, Vue.js, Angular, Next.js, Express.js, NestJS
- **Тестирование**: Jest, Mocha, Cypress
- **Версии**: Node.js 14-20

### Python
- **Сборщики**: pip, poetry, pipenv
- **Фреймворки**: Django, Flask, FastAPI, Streamlit
- **Тестирование**: pytest, unittest
- **Версии**: Python 3.7-3.11

## 🛠️ Требования

- Python 3.9+
- Git (опционально, для клонирования репозиториев)
- Docker и Docker Compose (для локальной инфраструктуры)

## 📦 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository_url>
cd self-deploy
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. (Опционально) Запустите локальную CI/CD инфраструктуру:
```bash
docker-compose up -d
```

## 🚀 Использование

### Режим 1: Интерактивный (рекомендуется)

Просто запустите скрипт без аргументов:

```bash
python self_deploy.py
```

**Что произойдет:**

1. Отобразится приветственное сообщение
2. Система запросит URL репозитория
3. Выполнится анализ и генерация пайплайна
4. Предложит проанализировать еще один репозиторий

**Пример интерактивной сессии:**

```
================================================================================
  🚀 Self-Deploy: CI/CD Pipeline Generator
================================================================================

Автоматическая генерация GitLab CI пайплайнов
на основе анализа структуры Git-репозитория.

📋 Поддерживаемые языки:
   • Java/Kotlin (Maven, Gradle)
   • Go (Go Modules)
   • TypeScript/JavaScript (npm, yarn, pnpm)
   • Python (pip, poetry, pipenv)

💡 Примеры URL:
   • https://github.com/spring-projects/spring-boot
   • https://gitlab.com/gitlab-org/gitlab
================================================================================

Введите URL Git-репозитория (или 'exit' для выхода):
> https://github.com/gin-gonic/gin

🔍 Анализируем репозиторий...

[Выполняется анализ...]

✅ Анализ завершен успешно!
Результаты сохранены в: generated_pipelines/gin/

Хотите проанализировать еще один репозиторий? (y/n):
> y

Введите URL Git-репозитория (или 'exit' для выхода):
> https://github.com/pallets/flask

[Выполняется анализ...]

✅ Анализ завершен успешно!

Хотите проанализировать еще один репозиторий? (y/n):
> n

👋 Спасибо за использование Self-Deploy!
```

**Команды в интерактивном режиме:**

- `exit`, `quit`, `q` - выход из программы
- `y`, `yes`, `да` - продолжить анализ
- `n`, `no`, `нет` - завершить работу
- `Ctrl+C` - прервать выполнение

### Режим 2: С аргументами (для автоматизации)

Для автоматизации и скриптов используйте режим с аргументами:

```bash
# Анализ одного репозитория
python self_deploy.py https://github.com/spring-projects/spring-boot

# С указанием директории для результатов
python self_deploy.py https://github.com/gin-gonic/gin -o my_pipelines

# С отладочным выводом
python self_deploy.py https://github.com/pallets/flask --debug
```

### Примеры для разных языков

```bash
# Java Spring Boot проект
python self_deploy.py https://github.com/spring-projects/spring-petclinic

# Go веб-фреймворк
python self_deploy.py https://github.com/gin-gonic/gin

# Python Flask фреймворк
python self_deploy.py https://github.com/pallets/flask

# TypeScript React проект
python self_deploy.py https://github.com/facebook/create-react-app

# GitLab репозиторий
python self_deploy.py https://gitlab.com/gitlab-org/gitlab
```

### Что происходит при выполнении

1. **Клонирование репозитория** (или загрузка через API)
2. **Анализ структуры файлов**:
   - Определение языка программирования
   - Поиск конфигурационных файлов (package.json, pom.xml, go.mod и т.д.)
   - Определение фреймворков и зависимостей
   - Определение версий
   - Поиск тестов и Dockerfile

3. **Генерация пайплайна**:
   - Выбор подходящего шаблона
   - Настройка этапов сборки, тестирования, анализа
   - Добавление Docker сборки (если есть Dockerfile)
   - Настройка развертывания

4. **Сохранение результатов**:
   - `.gitlab-ci.yml` - готовый пайплайн
   - `analysis_report.txt` - отчет об анализе
   - `README.md` - инструкции по использованию

## 📊 Структура сгенерированного пайплайна

Каждый пайплайн включает следующие этапы:

### 1. Build (Сборка)
- Установка зависимостей с кешированием
- Компиляция кода
- Создание артефактов

### 2. Test (Тестирование)
- Запуск unit-тестов
- Генерация отчетов о покрытии
- Публикация результатов в GitLab

### 3. Quality (Качество кода)
- Статический анализ кода (ESLint, Flake8, Checkstyle и т.д.)
- Проверка форматирования
- SonarQube анализ

### 4. Package (Упаковка)
- Создание архивов с артефактами
- Загрузка в Nexus Repository

### 5. Docker Build (если есть Dockerfile)
- Multi-stage сборка Docker образа
- Push в Container Registry
- Сканирование безопасности с Trivy

### 6. Deploy Staging/Production
- Развертывание в Kubernetes
- Поддержка ручного подтверждения

## 🐳 Локальная CI/CD инфраструктура

### Запуск инфраструктуры

```bash
docker-compose up -d
```

### Сервисы

| Сервис | URL | Учетные данные |
|--------|-----|----------------|
| GitLab CE | http://localhost:8080 | root / SelfDeploy2024! |
| SonarQube | http://localhost:9000 | admin / admin |
| Nexus | http://localhost:8081 | admin / (см. логи) |

### Первоначальная настройка

#### GitLab
1. Откройте http://localhost:8080
2. Войдите с учетными данными root / SelfDeploy2024!
3. Создайте новый проект
4. Скопируйте сгенерированный `.gitlab-ci.yml` в проект

#### SonarQube
1. Откройте http://localhost:9000
2. Войдите с учетными данными admin / admin
3. Создайте новый проект
4. Сгенерируйте токен для CI/CD
5. Добавьте токен в GitLab CI/CD переменные

#### Nexus
1. Откройте http://localhost:8081
2. Получите начальный пароль:
   ```bash
   docker exec self-deploy-nexus cat /nexus-data/admin.password
   ```
3. Войдите с учетными данными admin / <пароль>
4. Настройте репозитории для артефактов

#### GitLab Runner
```bash
# Получите registration token из GitLab
# Settings > CI/CD > Runners

# Зарегистрируйте runner
docker exec -it self-deploy-gitlab-runner gitlab-runner register \
  --url http://gitlab:80 \
  --registration-token YOUR_TOKEN \
  --executor docker \
  --docker-image alpine:latest \
  --description "self-deploy-runner"
```

### Остановка инфраструктуры

```bash
docker-compose down
```

### Полная очистка (включая данные)

```bash
docker-compose down -v
```

## 📁 Структура проекта

```
self-deploy/
├── self_deploy.py              # Главный CLI скрипт
├── requirements.txt            # Python зависимости
├── docker-compose.yml          # Локальная инфраструктура
├── README.md                   # Документация
├── DEPLOYMENT_GUIDE.md         # Руководство по развертыванию
├── analyzers/                  # Модули анализа
│   ├── __init__.py
│   ├── repository_analyzer.py  # Анализ структуры репозитория
│   ├── tech_stack_detector.py  # Определение технологий
│   └── pipeline_generator.py   # Генерация пайплайнов
└── generated_pipelines/        # Результаты генерации
    └── <repo_name>/
        ├── .gitlab-ci.yml
        ├── analysis_report.txt
        └── README.md
```

## 🔍 Примеры сгенерированных пайплайнов

### Java Spring Boot проект

```yaml
stages:
  - build
  - test
  - quality
  - package
  - docker-build
  - deploy-staging
  - deploy-production

variables:
  MAVEN_OPTS: '-Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository -Xmx1024m'

build:
  stage: build
  script:
    - mvn clean compile
    - mvn package -DskipTests
  artifacts:
    paths:
      - target/*.jar
```

### Python Django проект

```yaml
stages:
  - build
  - test
  - quality
  - package

image: python:3.9-slim

before_script:
  - pip install -r requirements.txt

build:
  stage: build
  script:
    - python manage.py collectstatic --noinput
    - python manage.py check --deploy
```

### Go проект

```yaml
stages:
  - build
  - test
  - quality

image: golang:1.19-alpine

build:
  stage: build
  script:
    - go mod download
    - go build -v -o app ./...
  artifacts:
    paths:
      - app
```

## 🎓 Логика определения технологий

### Определение языка программирования

Система анализирует расширения файлов и подсчитывает строки кода:
- `.java`, `.kt` → Java/Kotlin
- `.go` → Go
- `.js`, `.ts`, `.jsx`, `.tsx` → JavaScript/TypeScript
- `.py` → Python

### Определение фреймворков

#### JavaScript/TypeScript
- Анализ `package.json` → dependencies и devDependencies
- Поиск характерных файлов: `next.config.js`, `vue.config.js`

#### Python
- Анализ `requirements.txt` и `pyproject.toml`
- Поиск `manage.py` (Django), `app.py` (Flask)

#### Java/Kotlin
- Парсинг `pom.xml` (Maven) → dependencies
- Анализ `build.gradle` (Gradle)

#### Go
- Парсинг `go.mod` → require секция

### Определение версий

- **Java**: из `<maven.compiler.source>` или `sourceCompatibility`
- **Go**: из `go 1.19` в go.mod
- **Python**: из `requires-python` в pyproject.toml
- **Node.js**: из `engines.node` в package.json

### Определение инструментов сборки

Поиск характерных файлов:
- `package.json` → npm
- `yarn.lock` → yarn
- `pom.xml` → maven
- `build.gradle` → gradle
- `go.mod` → go modules
- `requirements.txt` → pip

## 🔧 Настройка GitLab CI переменные

Добавьте следующие переменные в Settings > CI/CD > Variables:

```
SONAR_HOST_URL=http://sonarqube:9000
SONAR_TOKEN=<ваш_токен>
NEXUS_URL=http://nexus:8081
NEXUS_USER=admin
NEXUS_PASSWORD=<ваш_пароль>
CI_REGISTRY=<ваш_registry>
CI_REGISTRY_USER=<ваш_пользователь>
CI_REGISTRY_PASSWORD=<ваш_пароль>
```

## 📝 Примечания

### Ограничения

- Система анализирует только публичные репозитории или требует настройки SSH ключей
- Для приватных репозиториев используйте Git клонирование с авторизацией
- Некоторые экзотические конфигурации могут требовать ручной доработки

### Рекомендации

- Проверьте сгенерированный пайплайн перед использованием
- Адаптируйте команды развертывания под вашу инфраструктуру
- Настройте переменные окружения в GitLab CI/CD
- Протестируйте пайплайн на staging окружении



## ❓ FAQ

**Q: Как выйти из интерактивного режима?**
A: Введите `exit`, `quit` или нажмите `Ctrl+C`

**Q: Можно ли анализировать приватные репозитории?**
A: Да, но требуется настройка SSH ключей или токенов доступа

**Q: Что делать, если анализ завершился с ошибкой?**
A: Используйте флаг `--debug` для подробного вывода: `python self_deploy.py --debug`

**Q: Как добавить поддержку нового языка?**
A: Отредактируйте `analyzers/tech_stack_detector.py` и `analyzers/pipeline_generator.py`

**Q: Можно ли использовать в CI/CD пайплайне?**
A: Да, используйте режим с аргументами: `python self_deploy.py <URL>`

## 📄 Лицензия

MIT License


---

**Self-Deploy** - Автоматизация CI/CD без DevOps инженеров! 🚀